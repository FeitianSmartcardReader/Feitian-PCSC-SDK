# Third-Party Components

## opensc-pkcs11

OpenSC PKCS#11 module for Android. Requires OpenSSL prebuilts in libs/<abi>/.

Build: run `./scripts/setup_opensc_android.sh` first, then `./gradlew :thirdparty:opensc-pkcs11:assembleRelease`
