<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for BluetoothAdapter.getState(), isEnabled() - some devices need this even on Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- stopScan() requires BLUETOOTH_ADMIN on some devices/Android versions -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <!-- Android 6–11: BLE scan requires location permission -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: app runs on devices without BLE/USB (listReaders returns empty) -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_preferences"
        android:label="@string/app_name"
        android:theme="@style/Theme.FTReaderPCSC">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- USB: when user connects Feitian reader, app can request permission -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".PcscTestActivity" android:exported="false" />
        <activity android:name=".PrivateApiTestActivity" android:exported="false" />
        <activity android:name=".Pkcs11HubActivity" android:exported="false" />
        <activity android:name=".PKCS11TestActivity" android:exported="false" />
        <activity android:name=".PKCS11DemoActivity" android:exported="false" />
    </application>
</manifest>
